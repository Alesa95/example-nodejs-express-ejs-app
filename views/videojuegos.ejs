<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>

    <table class="table table-inverse">
        <thead>
          <tr>
            <th>Título</th>
            <th>Consola</th>
          </tr>
        </thead>
        <tbody>
          <% for(var i=0; i < videojuegos.length; i++) { %>
          <tr>
            <td><%= videojuegos[i].titulo %></td>
            <td><%= videojuegos[i].consola %></td>
          </tr>
          <% } %>
        </tbody>
      </table>
  </body>
</html>
